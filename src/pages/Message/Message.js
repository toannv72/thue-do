import classNames from "classnames/bind";
import style from './Message.module.scss';


const cx = classNames.bind(style);
function Message() {
    return (
        <div className={cx('tinNhan')}>
            <h1>
             
            </h1>
        </div>
    );
}

export default Message;
